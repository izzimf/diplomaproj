# SentinelIoT — Полная инструкция по запуску

Полноценная минимальная рабочая система IoT IDS/IPS pipeline.

---

## 📋 Содержание

1. [Архитектура системы](#архитектура-системы)
2. [Предварительные требования](#предварительные-требования)
3. [Установка зависимостей](#установка-зависимостей)
4. [Настройка сети](#настройка-сети)
5. [Подготовка ML модели](#подготовка-ml-модели)
6. [Запуск ML Backend](#запуск-ml-backend)
7. [Запуск Traffic Pipeline](#запуск-traffic-pipeline)
8. [Примеры использования](#примеры-использования)
9. [End-to-End Pipeline объяснение](#end-to-end-pipeline-объяснение)
10. [Troubleshooting](#troubleshooting)

---

## 🏗️ Архитектура системы

```
┌─────────────────┐
│  IoT Devices    │
│ 192.168.1.0/24  │
└────────┬────────┘
         │
         │ (eth1)
         ▼
┌─────────────────────────────────────────┐
│         Orange Pi (Gateway)             │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │  1. tcpdump (capture.sh)         │  │
│  │     capture.pcap                 │  │
│  └──────────────┬────────────────────┘  │
│                 │                        │
│  ┌──────────────▼────────────────────┐  │
│  │  2. CICFlowMeter                 │  │
│  │     (run_cicflowmeter.sh)        │  │
│  │     flows.csv                    │  │
│  └──────────────┬────────────────────┘  │
│                 │                        │
│  ┌──────────────▼────────────────────┐  │
│  │  3. flow_sender.py               │  │
│  │     HTTP POST → ML Backend       │  │
│  └──────────────┬────────────────────┘  │
│                 │                        │
│  ┌──────────────▼────────────────────┐  │
│  │  4. ML Backend (FastAPI)          │  │
│  │     - model.pkl                  │  │
│  │     - scaler.pkl                 │  │
│  │     - Inference                  │  │
│  │     - block_ip() if anomaly      │  │
│  └──────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │  5. iptables (blocker.py)        │  │
│  │     Блокировка аномальных IP     │  │
│  └───────────────────────────────────┘  │
└──────────────┬──────────────────────────┘
               │ (eth0)
               ▼
         ┌─────────┐
         │ Router  │
         └────┬────┘
              │
              ▼
          Internet
```

---

## 📦 Предварительные требования

### Оборудование:
- Orange Pi (или другой Linux-совместимый SBC)
- Два сетевых интерфейса:
  - `eth0` (WAN) — подключение к роутеру
  - `eth1` (LAN) — подключение к IoT устройствам

### Программное обеспечение:
- Linux (Debian/Ubuntu)
- Python 3.11+
- Java 8+ (для CICFlowMeter)
- tcpdump
- CICFlowMeter
- Права root для сетевых операций

---

## 🔧 Установка зависимостей

### 1. Системные пакеты

```bash
# Обновление системы
sudo apt-get update
sudo apt-get upgrade -y

# Установка базовых инструментов
sudo apt-get install -y \
    python3.11 \
    python3-pip \
    python3-venv \
    tcpdump \
    iptables \
    dnsmasq \
    net-tools \
    iproute2 \
    curl \
    wget

# Установка Java (для CICFlowMeter)
sudo apt-get install -y default-jre
```

### 2. Установка CICFlowMeter

```bash
# Создаём директорию для CICFlowMeter
sudo mkdir -p /opt/CICFlowMeter
cd /opt/CICFlowMeter

# Скачиваем CICFlowMeter (замените на актуальную версию)
# Вариант 1: Клонируем репозиторий
sudo git clone https://github.com/ahlashkari/CICFlowMeter.git .

# Вариант 2: Скачиваем готовый JAR (если доступен)
# sudo wget https://github.com/ahlashkari/CICFlowMeter/releases/latest/download/CICFlowMeter.jar

# Устанавливаем переменную окружения
echo 'export CICFLOWMETER_DIR=/opt/CICFlowMeter' >> ~/.bashrc
echo 'export CICFLOWMETER_JAR=/opt/CICFlowMeter/CICFlowMeter.jar' >> ~/.bashrc
source ~/.bashrc
```

**Примечание:** Если CICFlowMeter требует компиляции, следуйте инструкциям в его репозитории.

### 3. Python зависимости для Backend

```bash
cd /path/to/diplomproj/backend

# Создаём виртуальное окружение (рекомендуется)
python3.11 -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Python зависимости для Traffic Pipeline

```bash
cd /path/to/diplomproj/traffic

# Используем то же виртуальное окружение или создаём новое
# Зависимости: pandas, numpy, requests (уже в requirements.txt backend)
```

---

## 🌐 Настройка сети

Следуйте инструкциям в файле `NETWORK_SETUP.md` для полной настройки сети.

**Краткая версия:**

1. Включите IP forwarding
2. Настройте интерфейсы (eth0 для WAN, eth1 для LAN)
3. Настройте NAT через iptables
4. Настройте DHCP через dnsmasq
5. Проверьте подключение IoT устройств

---

## 🤖 Подготовка ML модели

### Требования:
- Файл `model.pkl` — обученная RandomForest модель
- Файл `scaler.pkl` — обученный scaler (StandardScaler/MinMaxScaler)

### Размещение файлов:

```bash
# Скопируйте model.pkl и scaler.pkl в директорию backend/
cp /path/to/your/model.pkl /path/to/diplomproj/backend/
cp /path/to/your/scaler.pkl /path/to/diplomproj/backend/
```

### Проверка:

```bash
cd /path/to/diplomproj/backend
ls -lh model.pkl scaler.pkl
# Должны быть видны оба файла
```

---

## 🚀 Запуск ML Backend

### Вариант 1: Прямой запуск

```bash
cd /path/to/diplomproj/backend

# Активируем виртуальное окружение (если используется)
source venv/bin/activate

# Запускаем сервер
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### Вариант 2: Запуск в фоне (production)

```bash
cd /path/to/diplomproj/backend
source venv/bin/activate

# Запуск в фоне с логированием
nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 > backend.log 2>&1 &

# Проверка логов
tail -f backend.log
```

### Вариант 3: Systemd сервис (рекомендуется для production)

Создайте файл `/etc/systemd/system/sentinel-backend.service`:

```ini
[Unit]
Description=SentinelIoT ML Backend
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/diplomproj/backend
Environment="PATH=/path/to/diplomproj/backend/venv/bin"
ExecStart=/path/to/diplomproj/backend/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable sentinel-backend
sudo systemctl start sentinel-backend
sudo systemctl status sentinel-backend
```

### Проверка работы Backend:

```bash
# Проверка доступности API
curl http://127.0.0.1:8000/docs

# Или откройте в браузере
# http://your-orange-pi-ip:8000/docs
```

---

## 📡 Запуск Traffic Pipeline

### Шаг 1: Захват трафика (capture.sh)

```bash
cd /path/to/diplomproj/traffic

# Захват трафика на интерфейсе eth1 в течение 60 секунд
sudo ./capture.sh eth1 capture.pcap 60

# Или с другими параметрами
sudo ./capture.sh eth1 my_capture.pcap 120
```

**Параметры:**
- `eth1` — интерфейс для захвата (по умолчанию)
- `capture.pcap` — выходной файл (по умолчанию)
- `60` — таймаут в секундах (по умолчанию)

**Результат:** Файл `capture.pcap` с захваченным трафиком.

---

### Шаг 2: Конвертация в flows (run_cicflowmeter.sh)

```bash
cd /path/to/diplomproj/traffic

# Конвертация capture.pcap в flows.csv
./run_cicflowmeter.sh capture.pcap flows.csv

# Или с параметрами по умолчанию
./run_cicflowmeter.sh
```

**Параметры:**
- `capture.pcap` — входной pcap файл (по умолчанию)
- `flows.csv` — выходной CSV файл (по умолчанию)

**Результат:** Файл `flows.csv` с flow-данными.

**Примечание:** Убедитесь, что переменная `CICFLOWMETER_DIR` или `CICFLOWMETER_JAR` установлена правильно.

---

### Шаг 3: Отправка flows в ML Backend (flow_sender.py)

```bash
cd /path/to/diplomproj/traffic

# Активируем виртуальное окружение (если используется)
source ../backend/venv/bin/activate

# Отправка flows.csv в ML Backend
python3 flow_sender.py flows.csv

# Или с указанием URL backend
python3 flow_sender.py flows.csv --backend-url http://127.0.0.1:8000

# Для тестирования можно ограничить количество flow
python3 flow_sender.py flows.csv --limit 10
```

**Параметры:**
- `flows.csv` — входной CSV файл (по умолчанию)
- `--backend-url` — URL ML Backend (по умолчанию: `http://127.0.0.1:8000`)
- `--timeout` — таймаут HTTP запросов (по умолчанию: 10 секунд)
- `--limit` — ограничить количество обрабатываемых flow (для тестирования)

**Результат:** Каждый flow отправляется в ML Backend, получает оценку аномальности, при обнаружении аномалии IP блокируется через iptables.

---

## 🔄 Автоматизация Pipeline (опционально)

Создайте скрипт `/path/to/diplomproj/traffic/run_pipeline.sh`:

```bash
#!/bin/bash
set -e

INTERFACE="${1:-eth1}"
CAPTURE_TIME="${2:-60}"

echo "=========================================="
echo "Запуск SentinelIoT Pipeline"
echo "Интерфейс: $INTERFACE"
echo "Время захвата: ${CAPTURE_TIME} секунд"
echo "=========================================="

# Шаг 1: Захват трафика
echo "[1/3] Захват трафика..."
sudo ./capture.sh "$INTERFACE" capture.pcap "$CAPTURE_TIME"

# Шаг 2: Конвертация в flows
echo "[2/3] Конвертация в flows..."
./run_cicflowmeter.sh capture.pcap flows.csv

# Шаг 3: Отправка в ML Backend
echo "[3/3] Отправка в ML Backend..."
source ../backend/venv/bin/activate
python3 flow_sender.py flows.csv

echo "=========================================="
echo "Pipeline завершён!"
echo "=========================================="
```

Сделайте исполняемым:

```bash
chmod +x /path/to/diplomproj/traffic/run_pipeline.sh
```

Запуск:

```bash
./run_pipeline.sh eth1 60
```

---

## 📝 Примеры использования

### Пример 1: Тестовый запрос к ML Backend

```bash
curl -X POST "http://127.0.0.1:8000/flows/analyze" \
  -H "Content-Type: application/json" \
  -d '{
    "src_ip": "192.168.1.10",
    "ack_flag_number": 1,
    "HTTPS": 0,
    "Rate": 123.45,
    "Header_Length": 20,
    "Variance": 0.12,
    "Max": 10.0,
    "Tot sum": 100.0,
    "Time_To_Live": 64,
    "Std": 0.35,
    "psh_flag_number": 0,
    "Min": 1.0,
    "DNS": 0
  }'
```

**Ожидаемый ответ:**

```json
{
  "status": "ok",
  "risk_score": 0.4523,
  "is_anomaly": false,
  "threshold": 0.61,
  "src_ip": "192.168.1.10"
}
```

---

### Пример 2: Пример логов при обнаружении аномалии

**Логи ML Backend:**

```
2024-01-15 10:23:45 [INFO] app.main - Приложение стартует, загружаем ML модель и scaler...
2024-01-15 10:23:46 [INFO] app.ml.model_loader - Загружаем модель из /path/to/backend/model.pkl
2024-01-15 10:23:47 [INFO] app.ml.model_loader - Загружаем scaler из /path/to/backend/scaler.pkl
2024-01-15 10:23:47 [INFO] app.ml.model_loader - Модель и scaler успешно загружены.
2024-01-15 10:23:47 [INFO] app.main - ML модель и scaler успешно загружены.
2024-01-15 10:25:12 [WARNING] app.flows.routes - Обнаружена аномалия. IP=192.168.1.15, risk_score=0.7845 (> 0.61)
2024-01-15 10:25:12 [INFO] app.utils.blocker - Выполняем блокировку IP через iptables: sudo iptables -A INPUT -s 192.168.1.15 -j DROP
2024-01-15 10:25:12 [INFO] app.utils.blocker - IP 192.168.1.15 успешно заблокирован через iptables.
```

**Логи flow_sender.py:**

```
2024-01-15 10:25:10 [INFO] __main__ - Запуск flow_sender.py
2024-01-15 10:25:10 [INFO] __main__ - CSV файл: flows.csv
2024-01-15 10:25:10 [INFO] __main__ - Backend URL: http://127.0.0.1:8000
2024-01-15 10:25:10 [INFO] __main__ - Прочитано строк: 15, колонок: 80
2024-01-15 10:25:12 [INFO] __main__ - Ответ от backend: IP=192.168.1.15, risk_score=0.7845, is_anomaly=True
2024-01-15 10:25:12 [WARNING] __main__ - ⚠️  АНОМАЛИЯ обнаружена! IP=192.168.1.15, risk_score=0.7845
2024-01-15 10:25:15 [INFO] __main__ - Обработка завершена
2024-01-15 10:25:15 [INFO] __main__ - Всего flow: 15
2024-01-15 10:25:15 [INFO] __main__ - Успешно обработано: 15
2024-01-15 10:25:15 [INFO] __main__ - Ошибок: 0
2024-01-15 10:25:15 [INFO] __main__ - Аномалий обнаружено: 1
```

**Проверка блокировки IP:**

```bash
# Проверка правил iptables
sudo iptables -L INPUT -n -v | grep 192.168.1.15

# Должно быть видно правило:
# DROP       all  --  192.168.1.15      0.0.0.0/0
```

---

## 🔄 End-to-End Pipeline объяснение

### Полный цикл обработки трафика:

1. **Захват трафика (tcpdump)**
   - `capture.sh` запускает `tcpdump` на интерфейсе `eth1`
   - Захватывает все пакеты, идущие от IoT устройств
   - Сохраняет в файл `capture.pcap` (бинарный формат)

2. **Конвертация в flows (CICFlowMeter)**
   - `run_cicflowmeter.sh` запускает CICFlowMeter (Java приложение)
   - CICFlowMeter анализирует `capture.pcap`
   - Группирует пакеты в flows (соединения)
   - Извлекает статистические признаки для каждого flow
   - Сохраняет в `flows.csv` (CSV формат с множеством признаков)

3. **Извлечение признаков (flow_sender.py)**
   - `flow_sender.py` читает `flows.csv` через pandas
   - Для каждой строки (flow) извлекает 12 нужных признаков:
     - `ack_flag_number`, `HTTPS`, `Rate`, `Header_Length`, `Variance`, `Max`, `Tot sum`, `Time_To_Live`, `Std`, `psh_flag_number`, `Min`, `DNS`
   - Обрабатывает NaN значения (заменяет на 0.0)
   - Извлекает IP источника (`src_ip`)

4. **ML Inference (ML Backend)**
   - `flow_sender.py` отправляет HTTP POST запрос на `/flows/analyze`
   - FastAPI валидирует данные через Pydantic
   - `inference.py`:
     - Формирует DataFrame из признаков
     - Применяет scaler для нормализации
     - Выполняет предсказание через RandomForest модель
     - Получает `risk_score` (0-1)
     - Определяет `is_anomaly = (risk_score > 0.61)`

5. **Блокировка (blocker.py)**
   - Если `is_anomaly == True`:
     - Логируется WARNING с IP и risk_score
     - Вызывается `block_ip(src_ip)`
     - `blocker.py` выполняет команду:
       ```bash
       sudo iptables -A INPUT -s <ip> -j DROP
       ```
     - IP адрес блокируется на уровне ядра Linux

6. **Результат**
   - Backend возвращает JSON с результатом анализа
   - `flow_sender.py` логирует результат
   - При обнаружении аномалии IP блокируется через iptables
   - Дальнейший трафик с заблокированного IP отбрасывается

### Поток данных:

```
Raw Packets (eth1)
    ↓
capture.pcap (tcpdump)
    ↓
flows.csv (CICFlowMeter)
    ↓
12 features + src_ip (flow_sender.py)
    ↓
HTTP POST → ML Backend
    ↓
DataFrame → Scaler → Model
    ↓
risk_score, is_anomaly
    ↓
if is_anomaly:
    block_ip(src_ip) → iptables DROP
```

---

## 🐛 Troubleshooting

### Проблема: Backend не запускается

**Решение:**
```bash
# Проверьте наличие model.pkl и scaler.pkl
ls -lh backend/model.pkl backend/scaler.pkl

# Проверьте логи
tail -f backend.log

# Проверьте, что порт 8000 свободен
sudo netstat -tulpn | grep 8000
```

### Проблема: CICFlowMeter не работает

**Решение:**
```bash
# Проверьте Java
java -version

# Проверьте путь к CICFlowMeter
echo $CICFLOWMETER_JAR

# Проверьте логи
cat cicflowmeter.log
```

### Проблема: flow_sender.py не может подключиться к Backend

**Решение:**
```bash
# Проверьте, что Backend запущен
curl http://127.0.0.1:8000/docs

# Проверьте URL в flow_sender.py
python3 flow_sender.py flows.csv --backend-url http://127.0.0.1:8000
```

### Проблема: IP не блокируется

**Решение:**
```bash
# Проверьте флаг ENABLE_BLOCKING
grep ENABLE_BLOCKING backend/app/utils/blocker.py

# Проверьте права на выполнение iptables
sudo iptables -L -n

# Проверьте логи Backend
tail -f backend.log | grep blocker
```

### Проблема: Нет трафика в capture.pcap

**Решение:**
```bash
# Проверьте интерфейс
ip link show eth1

# Проверьте наличие трафика
sudo tcpdump -i eth1 -c 10

# Убедитесь, что IoT устройства подключены и генерируют трафик
```

---

## 📚 Дополнительные ресурсы

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [CICFlowMeter GitHub](https://github.com/ahlashkari/CICFlowMeter)
- [iptables руководство](https://www.netfilter.org/documentation/)
- [dnsmasq документация](https://thekelleys.org.uk/dnsmasq/doc.html)

---

## ✅ Чеклист запуска

- [ ] Установлены все системные зависимости
- [ ] Установлен и настроен CICFlowMeter
- [ ] Настроена сеть (IP forwarding, NAT, DHCP)
- [ ] Скопированы model.pkl и scaler.pkl в backend/
- [ ] Установлены Python зависимости
- [ ] Запущен ML Backend
- [ ] Проверена работа Backend через curl
- [ ] Запущен capture.sh и получен capture.pcap
- [ ] Запущен run_cicflowmeter.sh и получен flows.csv
- [ ] Запущен flow_sender.py и проверены логи
- [ ] Проверена блокировка IP через iptables при обнаружении аномалии

---

**Готово! Система SentinelIoT полностью настроена и готова к работе.** 🎉

